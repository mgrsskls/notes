{% include "../common/form/form.twig" with {
  csrfToken: csrfToken,
  note: note,
  tags: tags,
  topics: topics,
  activeTags: activeTags,
  submit: {
    url: "/notes/" ~ note.id ~ "/edit",
    label: "note.update.submit"|t
  }
} only %}
