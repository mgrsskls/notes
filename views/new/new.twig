{% include "../common/form/form.twig" with {
  csrfToken: csrfToken,
  tags: tags,
  topics: topics,
  activeTags: activeTags,
  submit: {
    url: "/notes/new",
    label: "note.create.submit"|t
  }
} only %}
